import React from 'react'
import Typography from '@material-ui/core/Typography';

function LetraIndice(props){

    return(
        <Typography>
            {props.letraMain}
        </Typography>
    )
}

export default LetraIndice;